---
- hosts: ws
  become: yes
  gather_facts: no
  ignore_errors: yes
  tasks:
    - name:  Modify config file
      lineinfile:
        path: /etc/selinux/semanage.conf
        regexp: '^module-store ='
        line: 'module-store = false'